<%= form_for @utilite, :html => { :class => "form-horizontal utilite" } do |f| %>

    <% if @utilite.errors.any? %>
    <div id="error_expl" class="panel panel-danger">
      <div class="panel-heading">
        <h3 class="panel-title"><%= pluralize(@utilite.errors.count, "error") %> prohibited this utilite from being saved:</h3>
      </div>
      <div class="panel-body">
        <ul>
        <% @utilite.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
        </ul>
      </div>
    </div>
  <% end %>

  <div class="control-group">
    <%= f.label :title, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :title, :class => 'form-control' %>
    </div>
    <%= error_span(@utilite[:title]) %>
  </div>
  <div class="control-group">
    <%= f.label :avatar, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :avatar, :class => 'form-control' %>
    </div>
    <%= error_span(@utilite[:avatar]) %>
  </div>


  <%= f.submit nil, :class => 'btn btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
            utilites_path, :class => 'btn btn-default' %>

<% end %>
