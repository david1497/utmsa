<%= form_for(@anounce) do |f| %>
  <% if @anounce.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@anounce.errors.count, "error") %> prohibited this anounce from being saved:</h2>

      <ul>
      <% @anounce.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :titlu %><br>
    <%= f.text_field :titlu %>
  </div>
  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_field :content %>
  </div>
  <div class="field">
    <%= f.label :grupe_id %><br>
    <%= f.number_field :grupe_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
